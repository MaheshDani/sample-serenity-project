<?xml version="1.0"?>
<!DOCTYPE suppressions PUBLIC "-//Puppy Crawl//DTD Suppressions 1.1//EN"
        "http://www.puppycrawl.com/dtds/suppressions_1_1.dtd">
<suppressions>
    <!-- Suppress all checks for properties files -->
    <suppress checks="[a-zA-Z0-9]*" files="pom.xml"/>

    <!-- Suppress all checks for properties files -->
    <suppress checks="[a-zA-Z0-9]*" files=".*.properties"/>

    <!-- Suppress all checks for properties files -->
    <suppress checks="[a-zA-Z0-9]*" files="package-info.java"/>
    
    <!-- Suppress all checks for properties files -->
    <suppress checks="[a-zA-Z0-9]*" files="Parallel.*.java"/>

</suppressions>
